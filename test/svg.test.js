import  SVG from '../src/svg';

const sveg = SVG;
describe('tests to GetAllProject', () => {
  test('should add todo for a project', () => {
    const getAllProject =sveg.deleteBtn() ;
    expect(getAllProject).toBe(getAllProject);
  });
});
describe('tests to GetAllProject', () => {
  test('should add todo for a project', () => {
    const getAllProject =sveg.editBtn() ;
    expect(getAllProject).toBe(getAllProject);
  });
});